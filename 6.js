// Comparison Operators - always evaluate to a boolean value
const total = 30;
const otherTotal = 40;

console.log(3 < 2); // false
console.log(total <= 3); // false

console.log(total >= 30); // true

const.log(total === otherTotal); // false

// "=" is more of a statement or declaration of a value and "===" is more of a question asking "are these two numbers equal?"

